module.exports = testpage;

testpage.$inject = ['PluS_express'];
function testpage(express) {
  var exp = {}
  exp.start = start;

  function start() {
    var app = express.app;
  
    app.get('/test', function (req, res) {
      res.send('New Page through Plugin');
    });
  }
  
  return exp;
}
